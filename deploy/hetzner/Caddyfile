# Set GALLERY_API_HOST to your real hostname before starting Caddy.
{$GALLERY_API_HOST} {
    reverse_proxy 127.0.0.1:8787
    header Strict-Transport-Security "max-age=31536000"
    # No access log: inquiry bodies, email addresses and questions are not logged.
}
